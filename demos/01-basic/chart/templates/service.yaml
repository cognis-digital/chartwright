{{- if .Values.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Chart.name }}
spec:
  ports:
    - port: {{ .Values.service.port }}
{{- else }}
# service disabled
{{- end }}
